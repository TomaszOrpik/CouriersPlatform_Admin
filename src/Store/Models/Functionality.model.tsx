import { Position } from "./Position.model";

export interface Functionality {
  canWrite: boolean;
  canRead: boolean;
  incorrectLoginInputMessage: string;
  sideBarVisible: boolean;
  confirmationModalVisible: boolean;
  mapPopupVisible: boolean;
}

export { };
